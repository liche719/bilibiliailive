use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "ai-live-local-search-only-change-before-public-deployment"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json
